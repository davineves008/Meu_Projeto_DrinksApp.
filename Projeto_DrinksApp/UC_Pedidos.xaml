<UserControl x:Class="Projeto_DrinksApp.UC_Pedidos"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#1A1A2E">
        <StackPanel Margin="20">
            <TextBlock Text="Meus Pedidos" FontSize="24" Foreground="White" Margin="0,0,0,20"/>
            <DataGrid x:Name="dgpedidos" 
          AutoGenerateColumns="False" 
          Background="Transparent" 
          RowBackground="#33FFFFFF" 
          Foreground="White"
          BorderThickness="0"
          CanUserAddRows="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nº Pedido" Binding="{Binding IdPedido}" Width="80"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding Data}" Width="150"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalGeral, StringFormat='R$ {0:F2}'}" Width="100"/>
                    <DataGridTextColumn Header="Observação" Binding="{Binding Observacao}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>