 <Window x:Class="Projeto_DrinksApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="500"
        Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E2E">

    <Window.Resources>

        <!-- BOTÃO ARREDONDADO -->
        <Style x:Key="BotaoArredondado" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="Black"/>
                <GradientStop Color="#FF133AD4" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Background="#2A2A40"
                Width="350"
                Height="380"
                CornerRadius="15"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <StackPanel Margin="30">

                <TextBlock Text="LOGIN"
                           FontSize="28"
                           Foreground="White"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <TextBlock Text="Usuário"
                           Foreground="White"
                           FontSize="14"/>

                <!-- TEXTBOX ARREDONDADO -->
                <Border CornerRadius="10"
                        Background="#3A3A55"
                        Margin="0,5,0,15">
                    <TextBox x:Name="txtUsuario"
                             Height="35"
                             FontSize="14"
                             Padding="10"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="White"
                             TextChanged="txtUsuario_TextChanged"/>
                </Border>

                <TextBlock Text="Senha"
                           Foreground="White"
                           FontSize="14"/>

                <!-- PASSWORDBOX ARREDONDADO -->
                <Border CornerRadius="10"
                        Background="#3A3A55"
                        Margin="0,5,0,25">
                    <PasswordBox x:Name="txtSenha"
                                 Height="35"
                                 FontSize="14"
                                 Padding="10"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Foreground="White"/>
                </Border>

                <Button Content="Entrar"
                        Height="40"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Style="{StaticResource BotaoArredondado}"
                        Click="BtnLogin_Click"/>

                <Button Content="Criar Conta"
                        Height="35"
                        Margin="0,15,0,0"
                        Background="Transparent"
                        Foreground="White"
                        BorderBrush="White"
                        BorderThickness="1"
                        Cursor="Hand"
                        Style="{StaticResource BotaoArredondado}"
                        Click="BtnCadastro_Click"/>

            </StackPanel>

        </Border>

    </Grid>

</Window>

